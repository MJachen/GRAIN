# Auditable metadata artifacts

This directory stores metadata only: environment reports, source-data hashes,
row/column counts and provenance records. It must never contain copied patient
features, CT volumes, legacy checkpoints or legacy result files.

